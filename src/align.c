#include <unistd.h>

#include "../include/alloc.h"

/**
 * @brief This function align number or memory address to 16
 * ex: size=24 -> return 32
 * @param size The number to align
 * @return Aligned number
 */
size_t	align_16(size_t size)
{
	(void) size;
	return (0);
}
